# @vercel/analytics pulls in an optional @sveltejs/kit peer (unused in this
# Nuxt/Vue app) that resolves to vite 8 and collides with Nuxt's vite 7.
# Relaxing peer strictness lets local, CI and the Vercel build install cleanly.
legacy-peer-deps=true
